import os
import lit.formats

config.test_source_root = os.path.dirname(__file__)

config.name = "SCHNITZEL"
config.suffixes = [".txt", ".mlir", ".py"]
root = os.path.dirname(config.test_source_root)
config.test_format = lit.formats.ShTest(preamble_commands=[f"cd {root}"])
